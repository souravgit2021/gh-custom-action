name: 'Deploy to AWS S3'
description: 'deploy a Static website via AWS S3'
inputs:
   bucket:
      description: 'The s3 bucket name'
      required: true
   bucket-region:
     description: 'The region of the s3 bucket'
     required: false
     default: 'us-east-2'
   dist-folder:
     description: 'deployeble file' 
     required: true  
runs:
  using: 'node16'
  main: 'main.js'
